<b>
Here's what the text file contains:

>>> @fake-command /u %1 /p %2

Here's what the command line looks like:

>>> test.cmd admin P@55w0rd > test-log.txt

The %1 applies to the first parameter the %2 (and here's the tricky part) applies to the second. 

You can have up to 9 parameters passed in this way.
</b>
